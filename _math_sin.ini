namespace = math\sin

; ---------------------------DESCRIPTION---
; cos(in)
; Returns a Float value or NaN
; -------------------------------EXAMPLE---
; $\math\cos\in = 40
; run = commandlist\math\cos\run
; x = $\math\cos\out
; ; x will be -0.666937

[Constants]
global $in
global $out

[CommandListRun]
local $pi = 3.14159265
$\math\cos\in = $in - $pi / 2
run = commandlist\math\cos\run
$out = $\math\cos\out
$in = 0

